spring:
#  cloud:
#    gateway:
#      routes:
#        - id: storage-service
#          uri: http://localhost:8081 # адрес твоего Storage-сервиса
#          predicates:
#            - Path=/api/storage/**  # все такие запросы отправляем туда
#          filters:
#            - RewritePath=/api/storage/(?<segment>.*), /${segment}
  config:
    import: optional:file:.env
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  sql:
    init:
      mode: ${SPRING_SQL_INIT_MODE:always}

server:
  port: ${SERVER_PORT:8081}

minio:
  bucket: ${MINIO_BUCKET}
  url: ${MINIO_URL}
  accessKey: ${MINIO_ACCESS_KEY}
  secretKey: ${MINIO_SECRET_KEY}